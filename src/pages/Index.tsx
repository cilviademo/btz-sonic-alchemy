import { CompactOnePage } from '@/components/vst/CompactOnePage';

const Index = () => {
  return (
    <div className="min-h-screen bg-background p-4 flex items-center justify-center">
      <CompactOnePage />
    </div>
  );
};

export default Index;